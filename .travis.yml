sudo: required
services:
    - docker

before_install:
    - docker build -t hen2864/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run hen2864/react-test npm test -- --coverage

after_success:
    - docker build -t hen2864/multi-client ./client
    - docker build -t hen2864/multi-nginx ./nginx
    - docker build -t hen2864/multi-server ./server
    - docker build -t hen2864/multi-worker ./worker
    # Log in to the docker CLI
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # Take those images and push them to docker hub
    - docker push hen2864/multi-client
    - docker push hen2864/multi-nginx
    - docker push hen2864/multi-server
    - docker push hen2864/multi-worker



    


